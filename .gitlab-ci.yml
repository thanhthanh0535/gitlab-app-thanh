stages:
  - build

test_npm:
  image: node:22-alpine
  stage: build
  script:
    - node --version
    - npm --version
    - ls -la
    - npm ci # same "npm install" but npm ci faster n using package json
    - npm run build 
    - ls -la